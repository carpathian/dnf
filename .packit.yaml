---
upstream_project_name: dnf
specfile_path: dnf.spec
jobs:
- job: copr_build
  trigger: pull_request
  metadata:
    targets:
    - fedora-30-x86_64

- job: tests
  trigger: pull_request
  metadata:
    targets:
    - fedora-30-x86_64
